dotnet:
  build:
    base:
      - dotnet@6
    build:
      - apt-get update
      - hostnamectl
      - dotnet --info
      - dotnet build -o app
    deploy:
      - app
  run:
    init:
      - apt-get update
      - hostnamectl
      - dotnet --info
    start: cd app && dotnet dnet.dll
